# Variables de entorno para producción en Netlify
# IMPORTANTE: El backend debe estar configurado con HTTPS o usar un proxy
# Opción 1: Backend con HTTPS (descomentar si el backend tiene certificado SSL)
# REACT_APP_API_BASE_URL=https://142.93.187.32:8000

# Opción 2: Usar proxy de Netlify (configurado en netlify.toml)
# El proxy redirige /api/* a http://142.93.187.32:8000/*
REACT_APP_API_BASE_URL=/api

REACT_APP_ENVIRONMENT=production
GENERATE_SOURCEMAP=false
CI=false